---
title: 'Multi-assignable Fragment'
---

```typescript
import { Feature, ProjectConfig } from "@patchworkdev/common/types";

const fragmentMultiProjectConfig: ProjectConfig = {
    name: "FragmentMultiProject",
    scopes: [
        {
            name: "test",
            whitelist: true,
            userAssign: false,
            userPatch: false,
        }
    ],
    contracts: {
        "FragmentMulti": {
            scopeName: "test",
            name: "FragmentMulti",
            symbol: "FM",
            baseURI: "https://mything/my/",
            schemaURI: "https://mything/my-metadata.json",
            imageURI: "https://mything/my/{tokenID}.png",
            fields: [
                {
                    id: 1,
                    key: "name",
                    type: "char32",
                    description: "Name"
                }
            ],
            features: [Feature.FRAGMENTMULTI]
        }
    }
};

export default fragmentMultiProjectConfig;

```
