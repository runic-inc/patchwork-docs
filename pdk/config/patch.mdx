---
title: 'Patch for ERC721 targets'
---

```typescript
import { Feature, ProjectConfig } from "@patchworkdev/common/types";

const patchProjectConfig: ProjectConfig = {
    name: "PatchProject",
    scopes: [
        {
            name: "test",
            whitelist: true,
            userAssign: false,
            userPatch: false,
        }
    ],
    contracts: {
        "Patch": {
            scopeName: "test",
            name: "Patch",
            symbol: "PATCH",
            baseURI: "https://mything/my/",
            schemaURI: "https://mything/my-metadata.json",
            imageURI: "https://mything/my/{tokenID}.png",
            fields: [
                {
                    id: 1,
                    key: "name",
                    type: "char32",
                    description: "Name"
                }
            ],
            features: [Feature.PATCH]
        }
    }
};

export default patchProjectConfig;

```
